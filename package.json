{
  "name": "bootstrap.purs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/aratama/bootstrap.purs"
  },
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "psc \"bower_components/purescript-*/src/**/*.purs\" \"src/**/*.purs\"",
    "build": "psc-bundle \"output/**/*.js\" --module Main --main Main --output public/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup-plugin-commonjs": "^7.0.0",
    "rollup-plugin-purs": "^1.0.16"
  }
}
